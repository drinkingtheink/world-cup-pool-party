# App environment variables
# Copy this to .env.local and fill in values.

# Secret passed to the Netlify update-matches function.
# Set the same value as UPDATE_SECRET in Netlify dashboard → Site settings → Env vars.
VITE_UPDATE_SECRET=your-secret-here
